<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>EveProduction</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="topbar">
    <div class="fill">
      <div class="container">
        <a class="brand" href="#">Eve Production</a>
        <ul class="nav">
          <li><%= link_to "Dashboard", root_path %></li>
          <% if signed_in? %>
            <li><%= link_to "Locations", locations_path %></li>
            <li><%= link_to "Characters", characters_path %></li>
            <li><%= link_to "My Blueprints", blueprints_path %></li>
          <% end %>
        </ul>

        <ul class="nav secondary-nav">
          <% if current_user %>
            <li class="dropdown" data-dropdown="dropdown">
              <a href="#" class="dropdown-toggle">Welcome, <%= current_user.name %>!</a>
              <ul class="dropdown-menu">
                <li><%= link_to "Sign Out", signout_path %></li>
              </ul>
            </li>
          <% else %>
          <li><%= link_to "Sign in with Google", "/auth/google_oauth2" %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>

  <div class="container">

    <div class="content">
      <div class="page-header">
        <h1>Page name <small>Supporting text or tagline</small></h1>
      </div>
      <div class="row">
        <div class="span16">
          <% flash.keys.each do |key| -%>
          <% klass = {:notice => :success}[key] %>
          <div class="alert-message <%= klass %>">
            <a class="close" href="#">×</a>
            <p><%= flash[key] %></p>
          </div>
          <% end -%>
          <%= yield %>
        </div>
      </div>
    </div>

    <footer>
      <p>&copy; T-P-L.com 2011</p>
    </footer>

  </div> <!-- /container -->

</body>
</html>
