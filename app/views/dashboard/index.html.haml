= form_tag({}, :method => :get, class: "form-inline") do
  %label.text
    Search:
    = text_field_tag "q", params[:q], :placeholder => "Search"
  = submit_tag "Go", class: "btn"
%ul
  - @items.each do |item|
    %li= link_to item.typename, :action => "info", :id => item.id
